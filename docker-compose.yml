services:
    postgres:
        image: postgres
        environment:
            POSTGRES_DB: orders
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: password
        ports:
            - 5432:5432